<%= form_for(current_user.follows.find_by(article_id: @article.id), html: { method: :delete }, remote: true) do |f| %>
  <%= hidden_field_tag :article_id, @article.id %>
  <button type="submit" class="btn btn-light btn-sm"><i class="fa fa-eye fa-fw red"></i>关注</button>
<% end -%>
