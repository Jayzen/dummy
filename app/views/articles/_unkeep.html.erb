<%= form_for(current_user.keeps.find_by(article_id: @article.id), html: { method: :delete }, remote: true) do |f| %>
  <%= hidden_field_tag :article_id, @article.id %>
  <button type="submit" class="btn btn-light btn-sm"><i class="fa fa-bookmark fa-fw red"></i>收藏</button>
<% end %>
