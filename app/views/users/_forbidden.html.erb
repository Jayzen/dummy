<%= form_for(@user, url: forbidden_user_path(@user), method: :post, remote: true) do |f| %>
  <input type="hidden" name="user_id" value="<%= @user.id %>" />
  <%= f.submit "用户禁止", class: "btn btn-secondary btn-sm" %>
<% end %>
